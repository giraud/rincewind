steps:
  - task: CopyFiles@2
    displayName: 'Copy exe'
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/master'))
    inputs:
      sourceFolder: $(Build.SourcesDirectory)/_release/final
      targetFolder: $(Build.ArtifactStagingDirectory)
      flattenFolder: true
